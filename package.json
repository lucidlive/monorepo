{
  "name": "@onyx/root",
  "private": true,
  "dependencies": {
    "lerna": "^4.0.0",
    "react": "^18.0.0",
    "craco": "^0.0.3"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "scripts": {
    "start": "lerna exec --scope @onyx/web-portal -- yarn start",
    "build": "lerna exec --scope @onyx/web-portal -- yarn build"
  },
  "engines": {
    "node": "^10.9.0"
  },
}
