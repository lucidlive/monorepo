{
  "name": "@onyx/root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna exec --scope @onyx/web-portal -- yarn start",
    "dev": "lerna exec --scope @onyx/web-portal -- yarn dev",
    "build": "yarn build customer-portal",
    "customer-portal:start": "yarn start customer-portal",
    "customer-portal:build": "yarn build customer-portal",
    "customer-portal:dev": "yarn dev customer-portal"
  },
  "dependencies": {
    "craco": "^0.0.3"
  },
  "packageManager": "yarn@3.2.0"
}
