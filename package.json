{
  "name": "@onyx/root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "scripts": {
    "start": "lerna exec --scope @onyx/web-portal -- yarn start",
    "build": "lerna exec --scope @onyx/web-portal -- yarn build"
  },
  "dependencies": {
    "craco": "^0.0.3"
  }
}
